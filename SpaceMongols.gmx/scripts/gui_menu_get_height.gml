/*
    Returns the total height, in pixels, of all of the components in this menu. Useful for checking if too many
    components have been added to the menu to fit on screen.
    
    argument0: menu
*/

with (argument0) {
    return height;
}
