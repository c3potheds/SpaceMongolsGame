///_build_menu_dataHandler(object, key, value)

var object = argument0;
var key = argument1;
var value = argument2;

with (object) {
    switch (key) {
        case 'name':
            object.name = name;
            break;
    }
}
